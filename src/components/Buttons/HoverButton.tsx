import { Button } from "@mui/material";
import React from "react";

const HoverButton = () => {
  return (
    <>
      <Button variant="contained" color="primary" sx={{ width: 40 }} >+</Button>
    </>
  );
};

export default HoverButton;
