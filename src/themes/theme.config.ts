import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#6DA305',
      dark: '#416103'
    },
    secondary: {
      main: '#A92C3U',
    },
  },
});

export default theme;